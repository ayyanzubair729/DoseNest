{
  "name": "dosenest",
  "version": "0.1.0",
  "private": true,
  "description": "DoseNest — Your medication companion for you and your family.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "format": "npm run format --workspace client && npm run format --workspace server",
    "test": "npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}